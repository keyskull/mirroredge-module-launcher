# Legacy code

Archived sources kept for reference only. **Not built** by `ModuleLauncher.sln` or `build.ps1`.

| Path | Replaced by |
|------|-------------|
| `legacy/mmultiplayer/` | `core/` + `engine/` |

Do not deploy or inject `legacy/mmultiplayer/mmultiplayer.dll`. Use `modules/core/core.dll` instead.
